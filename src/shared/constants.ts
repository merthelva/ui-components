enum VariantTypeEnum {
  primary = 'primary',
  secondary = 'secondary',
  info = 'info',
  success = 'success',
  warning = 'warning',
  error = 'error',
}

enum ComponentSizeEnum {
  small = 'small',
  medium = 'medium',
  large = 'large',
}

export { VariantTypeEnum, ComponentSizeEnum }
